function solution(s) {
    var answer = 0;
    if(s.includes("+")){
        answer=Number(s)
        
    }else if(s.includes("-")){
        answer=Number(s)
    }else{
        answer=Number(s)
    }
    return answer;
}